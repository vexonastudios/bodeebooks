export function dashboardNotice(message: string, status: number) {
  const escape = (value: string) => value.replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]!));
  const title = status === 401 ? 'Let’s reconnect.' : status >= 500 ? 'We’ll be right back.' : 'Your parent dashboard';
  const detail = status === 401 ? 'Your session needs a refresh. Try again to continue.' : status >= 500 ? 'BodeeGuard is temporarily unavailable. Please try again in a moment.' : message;
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>BodeeGuard · Reconnect</title>
<style>*{box-sizing:border-box}body{margin:0;min-height:100dvh;background:#0d0d1a;color:#f4f2ff;font:16px/1.6 system-ui,-apple-system,sans-serif;display:grid;place-items:center;padding:24px}main{width:min(100%,540px);padding:40px;background:#151426;border:1px solid #343148;border-radius:24px;box-shadow:0 24px 80px #0004}.brand{display:flex;align-items:center;gap:12px;font-size:19px;font-weight:750;color:#ddd6fe}.brand img{border-radius:12px}h1{font-size:clamp(26px,5vw,34px);line-height:1.2;margin:32px 0 14px}p{color:#c1bed4;margin:0 0 28px}.actions{display:flex;align-items:center;flex-wrap:wrap;gap:16px}a{color:#ddd6fe;text-underline-offset:4px}.retry{display:inline-flex;align-items:center;gap:9px;border:1px solid #42b992;background:#237f65;color:#fff;border-radius:11px;padding:12px 20px;text-decoration:none;font-weight:700;min-height:48px}a:focus-visible{outline:3px solid #a78bfa;outline-offset:5px}.retry:hover{background:#2b997a}@media(max-width:480px){main{padding:28px 24px}}</style>
</head><body data-status="${status}"><main><div class="brand"><img src="/guard-icons/bodeeguard-parent-192.png" width="40" height="40" alt="">BodeeGuard</div><h1>${title}</h1><p>${escape(detail)}</p><div class="actions"><a class="retry" href="/guard/dashboard/" target="_top"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M3 11a9 9 0 1 1 2.4 7M3 4v7h7"/></svg>Try again</a><a href="${status === 401 ? '/guard/sign-in/?redirect_url=%2Fguard%2Fdashboard%2F' : '/guard/account/'}" target="_top">${status === 401 ? 'Sign in' : 'Parent account'}</a></div></main><script src="/guard-admin/connection-recovery.js"></script></body></html>`;
}
